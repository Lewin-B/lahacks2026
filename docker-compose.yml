services:
  water-grid:
    build:
      context: ./water-grid-frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: unless-stopped

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - DRIP_SERVER_URL=http://100.101.97.92:3000
      - ASUS_INFERENCE_URL=http://100.89.241.84:5000/inference/drip-hub
    restart: unless-stopped
